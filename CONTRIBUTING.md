# 貢獻指南

感謝你想為《VibeCoding：從入門到精通》貢獻內容。本指南說明如何新增或修訂教材，讓每一篇都能被搜尋、分類並保持格式一致。

## 目錄

- [可以貢獻什麼](#可以貢獻什麼)
- [新增一個單元](#新增一個單元)
- [單元格式規範](#單元格式規範)
- [寫作原則](#寫作原則)
- [提交前檢查](#提交前檢查)
- [審核流程](#審核流程)

## 可以貢獻什麼

- 新增教材單元
- 修訂既有內容（錯字、過時資訊、不清楚的段落）
- 補充範例程式碼或練習題
- 回報問題（開 Issue，附上單元檔案與具體位置）

## 新增一個單元

1. 複製模板：`cp templates/unit-template.md docs/<你的-slug>.md`
2. 填寫 frontmatter 全部欄位（見下）。
3. 依模板段落撰寫內容，**不可省略**「動手練」與「完成檢核標準」。
4. 若是新單元，請把它加入對應的 `paths/*.yaml` 的 `units` 清單中，位置代表學習順序。
5. 執行檢查腳本，確認通過：`node scripts/check-units.mjs`

## 單元格式規範

每篇單元**必須**包含以下 frontmatter：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `title` | 字串 | 單元標題 |
| `slug` | 字串 | 唯一識別碼，需與檔名一致 |
| `level` | 列舉 | `beginner` / `intermediate` / `advanced` |
| `tags` | 字串陣列 | 主題標籤，取自下方標籤表 |
| `prerequisites` | 字串陣列 | 前置單元的 slug，沒有則空陣列 |
| `estimated_minutes` | 數字 | 預估完成時間（分鐘） |
| `path` | 列舉 | 所屬路徑：`beginner` / `intermediate` / `advanced` |
| `updated` | 日期 | `YYYY-MM-DD` |
| `maintainers` | 字串陣列 | 維護者 GitHub 帳號 |
| `status` | 列舉 | `active` / `stale` / `draft` |

**允許的標籤**：`入門`、`提示詞`、`除錯`、`工具鏈`、`工作流`、`部署`、`最佳實踐`、`實作專案`、`測試`、`資料`、`安全`、`架構`。

正文段落**必須**包含：學習目標、前置知識、為什麼重要、核心內容、常見坑、動手練、完成檢核標準、下一單元。

## 寫作原則

1. **具體可驗證**：避免「大概」「可能」等模糊詞；目標要能用行為描述。
2. **每個單元一個實作**：至少一道「動手練」，並附明確的完成檢核標準（checklist）。
3. **真誠不造假**：不確定的資訊標記「待確認」，不要杜撰 API 或數據。
4. **可重現**：程式碼範例需說明執行環境與預期輸出，且盡量只依賴標準函式庫。
5. **繁中為主**：專有名詞首次出現時附上英文原文。
6. **尊重版權**：引用外部資源請附來源連結。

## 提交前檢查

```bash
node scripts/check-units.mjs
```

檢查項目：

- frontmatter 必填欄位是否齊全、型別是否正確
- `slug` 是否與檔名一致
- `level`、`path`、`status` 是否為允許值
- `tags` 是否在允許清單內
- 單元內相對連結指向的檔案是否存在
- `paths/*.yaml` 引用的單元是否都有對應檔案

全部通過後再開 Pull Request。

## 審核流程

1. 貢獻者開 PR，說明新增/修訂內容。
2. 維護者檢查格式（CI 自動）與內容品質（人工）。
3. 至少一位維護者核准後合併。
4. 超過 12 個月未更新的單元，維護者可將 `status` 標記為 `stale` 並開 Issue 追蹤。
